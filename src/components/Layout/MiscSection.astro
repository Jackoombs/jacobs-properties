---
import clsx from "clsx";

interface Props {
  className?: string;
  bgColor?: string;
  disablePb?: boolean;
  disablePt?: boolean;
  overflowVisible?: boolean;
}

const {
  className,
  bgColor = "bg-white",
  overflowVisible = false,
  disablePb = false,
  disablePt = false,
}: Props = Astro.props;
---

<section
  class={clsx(
    "py-10 md:py-28 overflow-y-hidden",
    overflowVisible ? "overflow-x-visible" : "overflow-x-hidden",
    bgColor,
    disablePb && "pb-0 md:pb-0",
    disablePt && "pt-0 md:pt-0"
  )}
>
  <div class={clsx("mx-auto w-full max-w-container-lg", className)}>
    <slot />
  </div>
</section>
